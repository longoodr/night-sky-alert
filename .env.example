# Location - Choose ONE of the following options:

# Option 1: Use a location name (recommended - coordinates will be looked up automatically)
LOCATION=New York City, New York

# Option 2: Use exact coordinates (decimal degrees)
# If LOCATION is set, these are ignored
# LATITUDE=40.7128
# LONGITUDE=-74.0060

# Pushover notification credentials
PUSHOVER_USER_KEY=your_user_key
PUSHOVER_API_TOKEN=your_api_token

# Weather thresholds
CLOUD_COVER_LIMIT=15.0
PRECIP_PROB_LIMIT=5.0

# Minimum continuous viewing time required (in hours)
# Only send alert if there's at least this many consecutive hours of good weather
MIN_VIEWING_HOURS=1.0

# Check interval in minutes (1-60) for astronomical calculations
# Weather data is hourly, but astronomy is calculated at this interval
# Default: 15 minutes for good balance between performance and accuracy
# Lower values (1-5) provide smoother charts but slower execution
CHECK_INTERVAL_MINUTES=15

# Moon phase filtering (0.0 = new moon, 1.0 = full moon)
# Set MIN to 0.0 and MAX to 1.0 to allow all phases
# For dark sky viewing, try MAX_MOON_ILLUMINATION=0.3
MIN_MOON_ILLUMINATION=0.0
MAX_MOON_ILLUMINATION=1.0

# Optional: Custom viewing time window (24-hour HH:MM format, leave empty to use sunset/sunrise)
START_TIME=
END_TIME=
